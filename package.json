{
  "name": "jet2-modal-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel dist -d build",
    "sass": "node-sass -w scss/ -o dist/css/ --recursive",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "node-sass": "^4.13.1"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0"
  }
}
